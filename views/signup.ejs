<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="./css/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/tailcombinator.js"></script>
</head>

<body>
    <div class="flex justify-center">
        <div>
            <%- include('header.ejs') %>
            <p class="py-2"> <a href="/" class="text-green-600" >Go to Home page</a> </p>
                <h6>
                    Signup as a new user
                </h6>
                <form action="/users" method="post" class="">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="flex items-center justify-between gap-2">
                        <input type="text" name="firstName" placeholder="First Name" class="w-full" />
                        <input type="text" name="lastName" placeholder="Last Name" class="w-full" />
                    </div>
                    <input type="text" name="email" placeholder="Email" class="w-full" />
                    <input type="password" name="password" placeholder="Password" class="w-full" />
                    <button type="submit"
                        class="bg-green-600 rounded p-2 block w-full text-white hover:bg-green-700 transition mt-2">
                        Signup
                    </button>
                </form>
                <% if(messages && messages.error && messages.error.length> 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <li>
                            <%= messages.error[i] %>
                        </li>
                        <% } %>
                            <% } %>

                                <div class="text-center mt-6">
                                    Already have an account? <a href="/login" class="text-green-600 hover:text-green-700 transition">Login here</a>
                                </div>
        </div>

    </div>
    <script>
        tc.c("input", "block mt-2 p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-600 focus:border-transparent");
    </script>
</body>